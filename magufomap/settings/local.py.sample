from .common import *

# SECURITY WARNING: keep the secret key used in production secret!
# You can use this command to generate a new one:
# python -c "import random,string;print("".join([random.SystemRandom().choice(\"{}{}{}\".format(string.ascii_letters, string.digits, string.punctuation)) for i in range(63)]).replace("\\"","\\"\"\\"\"\\""))";
echo "DJANGO_SECRET_KEY="$(django_secret)""
SECRET_KEY = "$$$-type-your-production-secret-key-here-$$$"
DEBUG = False

